global:
  scrape_interval: 10s
  evaluation_interval: 10s


rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: finance-portal-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - backend:8080

